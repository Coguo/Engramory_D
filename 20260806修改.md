# 2026-08-14 修改整理(上传 GitHub 前)

基于 git 本地仓库**尚未提交**的工作区修改整理(相对 `4ae3c34` 0.5.0)。版本演进:0.5.0 → **0.6.0(全局库)** → 0.6.1(本次未单独建 tag,改动集中在两层模型的收敛 + `memory-init` skill)。

---

## 一、修改总览

| 维度 | 内容 |
|---|---|
| **主题** | 新增**全局记忆库**(host-agnostic、跨项目、跨 agent 的四类型记忆);文档改为**两层模型**;新增 **`memory-init`** 项目库脚手架 skill;确立 **`user` 只进全局库** 约定 |
| **涉及文件** | 12 个已修改 + 2 个未跟踪存档 + 1 个已删除(未跟踪,无 git 记录) |
| **净增** | +429 行 / -76 行 |
| **测试** | **89 tool + 35 hook 全绿**(含 5 个 home 用例 + 6 个 IGNORE 用例) |
| **设计定位** | 兼容旧版——项目库(`.engramory-memory/`、宿主原生 memory)不动,只在其上叠加全局库,再补一层项目库脚手架 |

---

## 二、核心新功能

### 1. 全局记忆库 `home` 模式(`tools/engramory_init.py`)

- 新增 `home` host,创建 `~/.engramory/`(默认,可用 `--memory-root` 迁移)。
- 与项目库一样是**纯四类型 markdown 文件夹**,但不属于任何单一项目,且**每个会话每个接入的 agent 都读**。
- **不创建** rules 文件 / gitignore / skill——各宿主通过自己的常驻规则各自接入:
  - **Claude Code**:粘贴 CLAUDE.md 片段到 `~/.claude/CLAUDE.md`
  - **Codex/OpenClaw**:注入的 rules 块已引用 `~/.engramory/`
  - **只读宿主**:`engramory_init.py <host>-reader --memory-root ~/.engramory`
- 拒绝 `--install-skill`(全局库无宿主 skill 目录,在**任何副作用前**报错)。
- `--memory-root` 的默认哨兵 `.engramory-memory` 双关:显式给定 → 用它;未给 → 落到 `~/.engramory`。
- 显示路径用 `~/` 形式(`~/.engramory`)。

### 2. 索引上限 hook 豁免 `ENGRAMORY_INDEX_IGNORE`(`hooks/engramory_index_guard.py`)

- 新增环境变量,逗号分隔的豁免列表,两种匹配:
  - **完整路径** → 只按解析后身份匹配(normcase + realpath)。豁免 `.../templates/MEMORY.md` **不会**连累同 basename 的真实索引 `memory/MEMORY.md` / `~/.engramory/MEMORY.md`。
  - **bare basename**(如 `MEMORY.md`)→ 匹配任何目录下同名文件,文档标注"慎用——会连豁免两层索引"。
- 意义:默认按文件名 `MEMORY.md` 匹配、天然守护**两层**索引;豁免只用于"某个 `MEMORY.md` 不是真索引"的情况(如本仓库 `templates/MEMORY.md`),而不会误伤真实索引。
- 配套 `_key()` 从 if 内提为 main 顶层,供 ignore 匹配复用。

### 3. 两层模型(`rules-snippet.md` + `SKILL.md`)

- **全局 tier** `~/.engramory/`:跨项目四类型(`user` / 跨项目 `feedback` / 跨项目 `reference`)。
- **项目 tier** `memory/`:本项目 `project` / 项目内 `feedback` / `reference`,**永不创建 `user`**。
- 回忆:任务开始读**两个**索引;写入:第 0 步**先定库**(跨项目→全局,本项目→项目;拿不准选项目 tier,因为全局索引每会话都加载、放错代价更高)。
- 上限:每个 `MEMORY.md` **各自独立**计数(150/200 行、20/25 KB)。
- hook 按文件名匹配 → 天然同时守护两层索引。

### 4. 项目库脚手架 `memory-init` skill(新增,部署在用户侧)

- 模板与脚本源在**用户侧** `~/.claude/skills/memory-init/` 与 `~/.engramory/templates/`,**不在本仓库内**(仓库内只有 INSTALL.md §4 对其的描述)。
- 作用:检测项目根 → 若无 `memory/MEMORY.md` 则从项目模板建库 → 建 `memory/` + `memory/feedback/`、`memory/project/`、`memory/reference/` 三个类型子文件夹 → 在项目 `CLAUDE.md` 追加标记块绑定双库 → `.gitignore` 加 `memory/` → 一句中文汇报("已完成项目记忆库搭建")。
- 幂等:重复运行不改动已有库;绝不触碰全局库 `~/.engramory/`;绝不建 `user` 注释。
- 细节文件按类型进子文件夹,索引指针带子文件夹路径(`- [title](project/xxx.md) — hook`)。

### 5. 关键约定: `user` 只进全局库

- **全局库保留全部四类型**;项目库只留 `feedback` / `project` / `reference`,永不创建 `user`。
- 理由:谁是用户永远是跨项目事实,放项目库会每项目复制一份然后漂移。
- 贯穿四处文档:`rules-snippet.md`(两处)、`SKILL.md`(§0 / "Which tier?" / §2 user 类型 / §5 写入第 0 步)、`hooks/INSTALL.md`、用户级 `~/.claude/CLAUDE.md`。

---

## 三、分文件改动明细

### 已修改(12)

| 文件 | 改动摘要 |
|---|---|
| [tools/engramory_init.py](tools/engramory_init.py) | 新增 `home` 模式:docstring、`_DEFAULT_MEMORY_ROOT` 哨兵、`_init_home()`、`init_host` 分流、parser choices + help |
| [rules-snippet.md](rules-snippet.md) | 改写为两层:引入全局库 + 项目库,双索引回忆,定库写入,各自上限;**追加 `user` 只进全局库约定(两处)** |
| [SKILL.md](SKILL.md) | §0 两层布局 + "Which tier?"、`user` 类型限定全局、§4 双索引回忆、§5 写入第 0 步定库 + 查重、§6 各自上限 + hook 双库、§7 项目 `memory/` 选项、§9 全局库接入;**同步补 `user` 只进全局库(§0 / §2 / §5)** |
| [hooks/engramory_index_guard.py](hooks/engramory_index_guard.py) | 新增 `ENGRAMORY_INDEX_IGNORE`(解析 + 路径/basename 分流匹配)+ docstring |
| [hooks/INSTALL.md](hooks/INSTALL.md) | §1 双库纪律说明、配置表加 `IGNORE` 行、两 opt-out 取舍、§4 双 tier 布局 + 双库守护说明;**§1/§4 补 memory-init skill 与 memory/ 布局** |
| [hooks/settings.snippet.json](hooks/settings.snippet.json) | 示例带 `ENGRAMORY_INDEX_IGNORE` 全路径 + `_comment` 说明 |
| [CHANGELOG.md](CHANGELOG.md) | 顶部加 0.6.0 条目(Added / Verified);Verified 数字待对齐为 **89 tool + 35 hook** |
| [README.md](README.md) | 新增「Global memory store」节 + 局限部分补全局库物理分库说明 |
| [README.zh-CN.md](README.zh-CN.md) | 同上,中文翻译 |
| [.gitignore](.gitignore) | 追加 `.engramory-memory/`(本仓库 self-host 记忆库) |
| [tests/test_tools.py](tests/test_tools.py) | +5 个 `home` 模式用例(创建 / 默认落 `~/.engramory` / 幂等保留 / 拒绝 skill / ascii 控制台) |
| [tests/test_index_guard.py](tests/test_index_guard.py) | +6 个 `IGNORE` 用例(路径豁免 / basename 豁免 / 不连累他文件 / 逗号列表 / **同 basename 不连累真实索引(回归)** / 空值 noop) |

### 未跟踪(2,存档对照)

| 文件 | 内容 |
|---|---|
| [SKILL-old.md](SKILL-old.md) | 旧版完整协议(四类型、单索引) |
| [rules-snippet-old.md](rules-snippet-old.md) | 旧版精简指针(单库 `<MEMORY_ROOT>/`) |

> 两个 old 文件与新版 `SKILL.md` / `rules-snippet.md` 同源,是**本次两库化改写前的旧版存档**,供对照。是否保留/归档为 `docs/archive/` 待你决定。

### 已删除(1,未跟踪)

- `templates/claude-engramory-snippet.md` —— 上一轮会话中删除(与 `rules-snippet.md` 功能重叠,且引用旧版原生自动记忆假设)。**磁盘与 git 均无痕迹**(从未被跟踪)。

---

## 四、⚠️ 待处理事项(整理时发现的真实问题)

### 1. 悬空引用:5 处指向已删除的 `templates/claude-engramory-snippet.md`

该文件已删,但以下位置仍引用它,导致 markdown 链接断裂:

- [CHANGELOG.md](CHANGELOG.md#L21) — "Added" 列表里作为 0.6.0 新产物
- [README.md](README.md#L157) — 全局库接入段
- [README.zh-CN.md](README.zh-CN.md#L78) — 全局库接入段
- [hooks/INSTALL.md](hooks/INSTALL.md#L17) — 提及 rules-snippet,相邻段落含 snippet 引用
- [SKILL.md](SKILL.md#L363) — "paste the CLAUDE.md snippet from `templates/`"

**建议处理**:引用改为指向 `rules-snippet.md`(它是实际的常驻规则来源),或新增一个简短 CLAUDE.md 接入片段模板。**此问题需你确认后我再改**。

### 2. 版本号漂移:`0.7.0` vs `0.6.0`

- [CHANGELOG.md](CHANGELOG.md) 顶栏:0.6.0(2026-08-06)
- [tests/test_index_guard.py](tests/test_index_guard.py#L328) 注释:`# --- 0.7.0: ENGRAMORY_INDEX_IGNORE exemptions ---`

hook 豁免这个改动在 CHANGELOG 里没单独成条目(0.6.0 条目未提及 `ENGRAMORY_INDEX_IGNORE`),而测试注释写成了 0.7.0。**建议**:要么把 hook 豁免并入 0.6.0 条目并把测试注释改 0.6.0,要么另起 0.7.0 条目。**此问题需你确认后我再改**。

### 3. 小项

- `.gitignore` 新增的是 `.engramory-memory/`,但新约定里项目库是 `memory/`(rules-snippet / SKILL / memory-init 里写的都是 `memory/`)。需确认是否也要忽略 `memory/`(若该目录只放记忆)。—— 注意 `.gitignore` 里已有 `MEMORY.md` + `!templates/MEMORY.md`。
- `CHANGELOG.md` 的 Verified 写 "89 tool + 29 hook tests pass",但当前 hook 测试已到 **35**(新增 6 个 IGNORE 用例,且 0.3.x 后 hook 计数有变动)。数字需更新。
- `CHANGELOG.md` 0.6.0 条目在 Added 里把 `templates/claude-engramory-snippet.md` 列为新产物——该文件已删,与问题 1 同源。

---

## 五、测试与验证(实测)

- `python tests/test_tools.py` — **89 全绿**(含 5 个新增 home 用例)
- `python tests/test_index_guard.py` — **35 全绿**(含 6 个新增 IGNORE 用例)
- 端到端(真实 hook + 真实 settings.json 豁免):
  - `templates/MEMORY.md`(豁免)→ 静默放行 ✓
  - `memory/MEMORY.md`(真实索引)→ **deny** ✓
  - `~/.engramory/MEMORY.md`(真实索引)→ **deny** ✓
- 用户级 `~/.claude/settings.json` 已注册 exec-form hook,`ENGRAMORY_INDEX_IGNORE` = 本仓库 `templates/MEMORY.md`;JSON 合法。

---

## 六、本次会话附加改动(在 0.6.0 文档之上的收敛)

> 这部分是 0.6.0 全局库落地后,针对**实际部署体验**做的收敛,也一并纳入本次整理:

1. **`user` 只进全局库** 约定写入 `rules-snippet.md` / `SKILL.md` / `hooks/INSTALL.md` / 用户级 `~/.claude/CLAUDE.md`(见二.5)。
2. **项目库按类型分子文件夹**:`memory/feedback/` · `memory/project/` · `memory/reference/`,索引指针带子文件夹路径。
3. **`memory-init` skill** 落地在用户侧,一键搭建项目库,报告用中文一句话(见二.4)。
4. **用户级 `~/.claude/CLAUDE.md`** 改为双库纪律:读 `~/.engramory/MEMORY.md` + `memory/MEMORY.md` 两个索引;写入按类型进子文件夹;项目库由 `memory-init` 建。
5. **全局/项目模板分离**:`~/.engramory/templates/` 下 `MEMORY_global_template.md`(四类型)与 `MEMORY_project_template.md`(三类型,无 `user`)。

> 说明:本次修改没有为 `memory-init` 创建**仓库内**文件——skill 本体在用户侧(`~/.claude/skills/memory-init/`),仓库只通过 `hooks/INSTALL.md` 描述它。若你想让 skill 可分发,需要把它的 `SKILL.md` 收进仓库(例如 `skills/memory-init/SKILL.md`),并把 `memory-init` 变成安装步骤的一部分。**是否入库待你决定。**
